/**
 * @deprecated Slash definitions now live on each feature under src/features.
 * Kept so old require("./commands/definitions") does not break.
 */
module.exports = {
  residualBuilders: [],
  commandBuilders: [],
  commands: [],
};
